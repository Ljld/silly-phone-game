# AGENTS.md

## Cursor Cloud specific instructions

This repository (`silly-phone-game`) is currently a blank scaffold with only a `README.md`. There are no services, dependencies, build steps, lint checks, or tests to run.

### Current state
- **No source code or application files** exist yet.
- **No package manager lockfile** (`package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, etc.).
- **No Docker/docker-compose** configuration.
- **No CI/CD** configuration.

### When code is added
Once the project is scaffolded, update this section with:
- How to install dependencies (e.g. `pnpm install`).
- How to run the dev server, lint, and tests.
- Any required environment variables or secrets.
- Database or external service requirements.
